#include "holberton.h"
#include <stdlib.h>

/**
 * name - description
 *
 * @param: description
 * Return: description
 */

void *malloc_checked(unsigned int b)
{
	void *pointer;

	pointer = malloc(b);
	if (pointer == NULL)
		exit(98);
	return (pointer);
}
