#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "holberton.h"

char *_strdup(char *str)
{
	char *dupe;

	dupe = malloc(strlen (str) + 1);
	if (dupe == NULL)
		return NULL;
	strcpy(dupe,str);
	return dupe;
}
